/**
 * Where in the world, spelled the same way twice.
 *
 * A country is stored as a code and shown as a name, so formatting one surface
 * and not another is how a WhatsApp message says `HR` while the invoice beside
 * it says Croatia. `address` is the formatter both use.
 */
export * from "./countries";
export * from "./address";
