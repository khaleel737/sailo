/**
 * What makes a shop worth a human's attention.
 *
 * One module today. It is a folder because the next thing to land beside it is
 * the buyer-side ladder — card testing, a single card across forty shops — and
 * that reads none of the same inputs.
 */
export * from "./signals";
