{
  "name": "@sailo/events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./stream": "./src/event-stream.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src || true",
    "test": "vitest run"
  },
  "dependencies": {
    "@sailo/rate-limit": "workspace:*",
    "redis": "^6.2.0",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@sailo/config": "workspace:*",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
