---
title: REST API
description: Read a Sailo shop's orders, products and contacts over HTTP. Cursor-paged, one envelope for every answer, OpenAPI 3.1.
---

import {
  ApiVersion,
  DefaultLimit,
  EndpointCount,
  EndpointIndex,
  EndpointReference,
  ErrorCodeTable,
  MaxBodyKb,
  MaxLimit,
} from "@/components/docs/endpoint-reference";
import { ApiBaseUrl, CurlExample, KeyLink } from "@/components/docs/kit";

Base URL <code><ApiBaseUrl /></code>. Everything returns `{ "data": … }`;
failures return `{ "error": { "code", "message" } }` with a matching HTTP
status. Responses carry a `sailo-version` header, currently <ApiVersion />.

## The endpoints

<EndpointIndex />

A machine-readable description of all <EndpointCount /> lives at
[`/api/v1/openapi.json`](/api/v1/openapi.json) — OpenAPI 3.1, no key required,
which is what you point Postman or an SDK generator at.

## Authenticating

Send the key in the `Authorization` header and nowhere else. A key in a query
string is written into every access log and proxy it passes through.

<CurlExample path="/shop" />

Keys are shown once, at creation, and stored hashed — there is no way to recover
one, so a lost key is rotated rather than looked up. Every key is read-only
unless you tick `write`, and a read-only key reaching a write endpoint is told
exactly that rather than given a bare 403.

A key we will not accept is refused identically whether it never existed, was
revoked, or belongs to a shop that has since been deleted. That is deliberate:
learning *which* would tell the holder of a token that it used to be real.

## Paging

Cursor-based, <DefaultLimit /> per page by default and up to <MaxLimit />. Pass
the `next_cursor` from one response as `?cursor=` on the next, and stop when
`has_more` is false. Loop on `has_more` rather than on
whether the cursor is null — the two answer different questions, and a consumer
branching on the cursor gets the wrong one on the last full page.

Cursors name a position in the ordering rather than an offset, so orders
arriving mid-scan cannot make you skip one. They are opaque; building anything
on their internals is building on something that will change. A cursor we did
not issue is an `invalid_request`, not an empty page.

Asking for more than <MaxLimit /> is clamped rather than refused, so a caller who
sends `limit=1000` gets <MaxLimit /> and a working integration.

## Errors

Branch on `code`. It is stable — one is never renamed, only added to — while
`message` is a sentence for a person and may be reworded at any time.

<ErrorCodeTable />

A `server_error` body never says what went wrong. A stack trace, a Postgres
message or a constraint name in a response is a description of our schema handed
to whoever provoked it.

## Limits

**240 requests a minute per key.** Per key rather than per address,
deliberately: an integration and a seller's browser routinely share an office
IP, and a Zap running flat out must not be able to throttle its owner out of
their own admin. High enough that no ordinary integration will meet it.

Requests that fail to authenticate are separately and more tightly limited, per
source address. That budget is for guessing, so its size is not published and
you should not calibrate against it — a client that has a valid key will never
encounter it, and one that is probing for keys learns nothing from the response
either way.

Request bodies are capped at <MaxBodyKb /> KB. There is no CORS on any of this:
keys are for servers, and a key a browser can send is a key in somebody's
bundle.

## Consent, on the contact endpoints

**Nothing here can mark anyone as consenting to marketing email.** A contact
created through the API always has `marketingConsentAt: null`, whatever you send
— consent is something a person gave, not something a request body can assert.
To get it, pass `"sendOptIn": true`: Sailo emails them the same double opt-in
link the public signup form uses, and consent is recorded when they click it.

Filter with `?consented=true` when syncing to a newsletter tool. Everyone else
on the list is a customer who never agreed to be emailed.

## Reference

Every endpoint, with what it takes and what it answers. Ids in the examples are
illustrative; yours are UUIDs.

<EndpointReference />

## Getting a key

Every key is read-only unless you tick `write`, and is shown once at creation.
Create one under <KeyLink />.
